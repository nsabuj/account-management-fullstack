{
  "name": "account-management-fullstack-level-1",
  "version": "0.0.1",
  "scripts": {


    "build:backend": "cd Api && pip3 install -r requirements.txt",
    "build:frontend": "react-scripts install & react-scripts build",
    "build": "npm run build:backend & npm run build:frontend",
    "start:backend": "cd Api && flask run",
    "start:frontend": "react-scripts start",
    "start": "npm run start:backend && npm run start:frontend",
    "test": "cypress run"

  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.9",
    "@testing-library/react": "^11.2.5",
    "@testing-library/user-event": "^12.8.3",
    "classnames": "^2.2.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.3",
    "uuid": "^3.4.0",
    "web-vitals": "^1.1.1"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
